import { LocalYearPipe } from './local-year.pipe';

describe('LocalYearPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalYearPipe();
    expect(pipe).toBeTruthy();
  });
});
