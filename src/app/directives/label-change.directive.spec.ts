import { LabelChangeDirective } from './label-change.directive';

describe('LabelChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelChangeDirective();
    expect(directive).toBeTruthy();
  });
});
