import { AgeChangeDirective } from './age-change.directive';

describe('AgeChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeChangeDirective();
    expect(directive).toBeTruthy();
  });
});
